/**
 * list
 */

import Element from './Element'
import util from './util'
import {
    t_spottedSeq
} from './types'

let match = (start: number, str: string): t_spottedSeq[] | false => {

}

let extract = (seqs: t_spottedSeq[], context: string): Element => {

}
